import React from 'react';

export default function Header() {
    return(
        <div className="Header">
            <h1>Memory Game</h1>
            <h3>Click each card, but only once. Click all the cards to win!</h3>
        </div>
    )
}

